# syntax=docker/dockerfile:1.7
#
# Production: bake the static p5.js sketch into the image so the running
# container has no host-mount dependency.

FROM nginx:alpine-slim

COPY . /usr/share/nginx/html

EXPOSE 80
